<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{BlogTitle}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }

        h1 {
            font-weight: 700;
            color: #343a40;
        }

        .blog-container {
            background-color: #ffffff;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin-top: 40px;
        }

        .blog-content {
            font-size: 1.2rem;
            line-height: 1.6;
            color: #495057;
        }

        .blog-author {
            font-size: 1rem;
            color: #6c757d;
            font-weight: 500;
        }

        .btn-outline-primary {
            padding: 10px 20px;
            border-radius: 5px;
            transition: 0.3s;
        }

        .btn-outline-primary:hover {
            background-color: #007bff;
            color: white;
            border-color: #007bff;
        }

        .back-btn-container {
            margin-top: 30px;
            text-align: center;
        }

        .comments-section {
            margin-top: 40px;
        }

        .list-group-item {
            background-color: #f1f1f1;
        }

        .like-count {
            margin-top: 10px;
            font-weight: 500;
        }
    </style>
</head>
<body>

    <div class="container mt-5 blog-container">
        {{#if success}}
    <script>
        alert("Blog added successfully!");
    </script>
    {{/if}}
        <h1 class="text-center mb-3">{{BlogTitle}}</h1>
        <p class="blog-content">{{Content}}</p>

        <p class="blog-author"><strong>Author:</strong> {{author}}</p>

        <!-- LIKE BUTTON -->
        <form action="/like/{{_id}}" method="POST" class="mt-4">
            <button type="submit" class="btn btn-outline-success btn-sm">👍 Like</button>
        </form>
        <p class="like-count">Likes: {{likes}}</p>

        <!-- COMMENT FORM -->
        <form action="/comment/{{_id}}" method="POST" class="mt-4">
            <div class="input-group">
                <input type="text" name="comment" class="form-control" placeholder="Add a comment..." required>
                <button class="btn btn-outline-secondary" type="submit">💬</button>
            </div>
        </form>

        <!-- COMMENTS LIST -->
        {{#if comments.length}}
        <div class="comments-section">
            <h5 class="mt-4">🗨️ Comments:</h5>
            <ul class="list-group mt-2">
                {{#each comments}}
                <li class="list-group-item">{{this}}</li>
                {{/each}}
            </ul>
        </div>
        {{/if}}

        <!-- BACK BUTTON AT THE END -->
        <div class="back-btn-container">
            <a href="/allblogs" class="btn btn-outline-primary mt-4">🔙 Back to All Blogs</a>
        </div>
    </div>

</body>
</html>

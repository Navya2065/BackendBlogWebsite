<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{BlogTitle}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }

        h1 {
            font-weight: 700;
            color: #343a40;
        }

        .blog-container {
            background-color: #ffffff;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin-top: 40px;
        }

        .blog-content {
            font-size: 1.2rem;
            line-height: 1.6;
            color: #495057;
        }

        .blog-author {
            font-size: 1rem;
            color: #6c757d;
            font-weight: 500;
        }

        .btn-outline-primary {
            padding: 10px 20px;
            border-radius: 5px;
            transition: 0.3s;
        }

        .btn-outline-primary:hover {
            background-color: #007bff;
            color: white;
            border-color: #007bff;
        }

        .back-btn-container {
            margin-top: 30px;
        }
    </style>
</head>
<body>

    <div class="container mt-5 blog-container">
        <h1 class="text-center mb-3">{{BlogTitle}}</h1>
        <p class="blog-content">{{Content}}</p>

        <div class="d-flex justify-content-between align-items-center mt-4">
            <p class="blog-author"><strong>Author:</strong> {{author}}</p>
            <div class="back-btn-container">
                <a href="/allblogs" class="btn btn-outline-primary">🔙 Back to All Blogs</a>
            </div>
        </div>
    </div>

</body>
</html>
